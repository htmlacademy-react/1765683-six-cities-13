export const Settings = {
  Offers: 5,
  isAuth: false,
};

export enum AppRoute {
  Main = '/',
  Login = '/login',
  Favorites = '/favorites',
  Offer = '/offer'
}

export enum OfferTypes {
  PrivateRoom = 'Private Room',
  Apartment = 'Apartment',
}
